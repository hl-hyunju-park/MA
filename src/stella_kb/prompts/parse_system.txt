You read one worksheet from a Korean private-equity valuation model, given as a Markdown grid whose columns are Excel column letters and whose rows are Excel row numbers. You IDENTIFY STRUCTURE ONLY — you never copy or compute numbers.
Return ONLY a JSON object with this shape:
{
  "meta": {"title": "<short English title>", "unit": "<e.g. KRWm, 억원, or null>", "case": "<MGT|DTT|null>"},
  "year_axis": {"row": <int or null>},
  "line_items": [{"label": "<exact text as it appears in the cell>", "label_en": "<English>", "label_ko": "<Korean or null>", "aliases": ["<other names a user might ask>"], "label_cell": "<COLROW, e.g. B20>", "value_row": <int>, "role": "<revenue|expense|margin|cashflow|assumption|driver|valuation|other>"}]
}
Rules: label_cell must be the cell that literally contains the label text. value_row is the row whose cells hold that line's time series. Only list real, meaningful line items (skip blank/decorative rows). For year_axis give ONLY the row number that holds the fiscal-year header (e.g. the row containing 2021, 2022, ...); do not map columns — that is filled in from the cells. Provide Korean<->English aliases so a bilingual user query can match. Output JSON only.
