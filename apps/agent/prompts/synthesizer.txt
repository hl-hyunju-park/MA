{{! Synthesizer prompt — assembled from composable blocks in blocks/ (see prompts/__init__.py). }}
{{! Each include is one reviewable rule block; order = final prompt order. }}
{{! To add a rule (e.g. the eval Fix 1+2 value-selection discipline): create }}
{{!   blocks/synth_value_selection.txt and add its include line in the right position. }}
{{include: blocks/synth_role}}

{{include: blocks/synth_final_calc}}

{{! synth_value_selection: A/B'd 2026-06-22 — no gain on its 6 targets, net regression; disabled. }}
{{! Re-enable by uncommenting; revisit as a retrieval/evidence fix, not a synthesis-prompt one. }}
{{include: blocks/synth_basis_mismatch}}

{{include: blocks/synth_audit}}

{{include: blocks/synth_output}}
